import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    container: {
        flex: 1,
        alignItems: 'center',
        width: '100%',
        height: '100%',
        backgroundColor: '#fff',
    },
    segmentedButtons: {
         width: '90%',
         margin: 10,
         padding: 10,
    },
});

export default styles;  